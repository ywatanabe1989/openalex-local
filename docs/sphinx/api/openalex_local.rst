openalex_local API
==================

The public API is generated automatically from the package's ``__all__``.
Each object is documented exactly once here via ``automodule`` (autodoc +
napoleon); do not add per-object ``autoclass``/``autofunction`` directives for
the same names below, or Sphinx will raise ``duplicate object description``
warnings (which fail the docs build under ``-W``).

.. automodule:: openalex_local
   :members:
   :undoc-members:
   :show-inheritance:
   :member-order: bysource
