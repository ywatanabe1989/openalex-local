# OpenAlex Local Configuration
# Copy this file to .env and customize as needed

# =============================================================================
# LOCAL MODE - Direct database access
# =============================================================================

# Path to OpenAlex SQLite database (optional - auto-detected if not set)
# OPENALEX_LOCAL_DB=/path/to/openalex.db

# =============================================================================
# DEVELOPMENT / DEBUGGING
# =============================================================================

# Enable debug mode for verbose logging
# OPENALEX_LOCAL_DEBUG_MODE=1

# Cache directory (default: ~/.cache/openalex-local)
# OPENALEX_LOCAL_CACHE_DIR=/path/to/cache

# =============================================================================
# REMOTE MODE - HTTP API access (via SSH tunnel or direct connection)
# =============================================================================

# Force remote mode (set to "http" or "db")
# OPENALEX_LOCAL_MODE=http

# API URL for remote mode (default: http://localhost:31292)
# OPENALEX_LOCAL_API_URL=http://localhost:31292

# Server host/port (for relay server)
# OPENALEX_LOCAL_HOST=0.0.0.0
# OPENALEX_LOCAL_PORT=31292

# =============================================================================
# SCITEX INTEGRATION (takes priority over OPENALEX_LOCAL_* variables)
# =============================================================================
# Port scheme: 3129X (31290: scitex-cloud, 31291: crossref, 31292: openalex)
#
# SCITEX_SCHOLAR_OPENALEX_DB=/path/to/openalex.db
# SCITEX_SCHOLAR_OPENALEX_MODE=http
# SCITEX_SCHOLAR_OPENALEX_HOST=0.0.0.0
# SCITEX_SCHOLAR_OPENALEX_PORT=31292

# =============================================================================
# TYPICAL SSH TUNNEL SETUP
# =============================================================================
# 1. Create SSH tunnel to your database server:
#    ssh -L 31292:127.0.0.1:31292 your-nas-server
#
# 2. Set environment variables:
#    export OPENALEX_LOCAL_MODE=http
#    export OPENALEX_LOCAL_API_URL=http://localhost:31292
#
# 3. Or use CLI flags:
#    openalex-local --http search "machine learning"
#    openalex-local --api-url http://localhost:31292 search "query"
